# 🗺️ 模板项目地图 (Template Navigation Map)
> **注意**: 本地图仅描述“协作模板母版”自身的结构。新生成的项目会有其独立的业务地图。

## 1. 核心地形 (Topology)

本项目是一个**“提示词资产工厂”**。

*   **📂 核心资产 (`prompts-library/`)**: 交付给用户的产品。
    *   `templates/capabilities/`: **原子能力** (产品/开发/测试/运维)。
    *   `templates/patterns/`: **协作模式** (如虚拟团队)。
    *   `guides/`: **使用说明书** (Prompt 编写指南)。
    *   `roles/`: **成品展示区** (用户直接可用的角色实例)。

*   **📂 维护工具 (`_meta/`)**: 维护本工厂的工具 (不会复制到新项目)。
    *   `prompts/ops/librarian.md`: **图书管理员**。负责从成品中逆向提取模板。
    *   `docs/`: 本项目的演进记录与设计文档。

*   **📂 运行时 (`.cursor/rules/`)**: AI 的行为准则。
    *   `virtual-streamlit-team.mdc`: 定义了如何动态加载 atomic capabilities。

*   **📄 启动器 (`generate-project.ps1`)**: 
    *   负责将 `prompts-library` 和基础设施复制到新目录，同时剔除 `_meta`。

## 2. 关键数据流 (Data Flow)

```mermaid
graph LR
    User[开发者] -->|1. 编写/优化| Roles[成品角色]
    Roles -->|2. 提取 (Librarian)| Templates[模板/原子能力]
    Templates -->|3. 组装 (Pattern)| NewTeam[新虚拟团队]
    NewTeam -->|4. 实例化| NewProject[新项目]
```

## 3. 维护者指南 (Maintainer Guide)
*   **添加新能力**: 在 `templates/capabilities` 下新建文件。
*   **添加新模式**: 在 `templates/patterns` 下新建引用文件。
*   **修改架构**: 必须同步更新 `README.md` 和本文件。

